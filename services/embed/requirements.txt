fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.7
sentence-transformers>=3.0
# torch is pulled in by sentence-transformers; the CPU build comes from the
# default index, the GPU build from the Dockerfile via TARGET=gpu.
